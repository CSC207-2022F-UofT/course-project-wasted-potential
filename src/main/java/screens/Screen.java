package screens;
import javafx.stage.Stage;

/**
 * The interface Screen. Implemented by all user interfaces.
 */
public interface Screen {
    /**
     * Starts the UI class.
     *
     * @param stage the stage
     */
    void start(Stage stage);
}